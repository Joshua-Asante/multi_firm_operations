# Live trading data — never commit
accounts.json
dd_protection_state.json
data/live/*.csv
*.json.bak

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Test artifacts (pytest-html-report and similar auto-generate here)
reports/
.pytest_cache/
.coverage
htmlcov/

# Env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# AUDNZD discovery loop raw bar CSVs (large; SHA256 sidecars are committed)
data/audnzd_oanda_m15_*.csv

# Live runtime data — DXTrade exports, journal outputs (size + churn + privacy)
live_journal/data/*.csv
live_journal/data/*.json
!live_journal/data/.gitkeep
