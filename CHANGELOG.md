# Changelog

**Scope:** repo, methodology, and tooling changes only. Per-strategy version history lives in `strategies/<name>/<name>_CHANGELOG.md` (e.g. `strategies/aegis/aegis_CHANGELOG.md`).

## 2026-05-08

- **NAS100 strategy-folder split + OANDA bar/panel coverage.** `striker_nas100_v1.pine` moved `strategies/striker/` → `strategies/nas/` (codifies the architecture-family-but-instrument-tuned distinction from DJ30: separate risk %, separate DOW set, separate pyramid size). New `strategies/nas/striker_nas100_CHANGELOG.md` mirrors the per-strategy convention. `scripts/fetch_oanda_bars.py` permanently extended to include NAS100USD; `data/bar_data/NAS100USD.csv` fetched from OANDA M15 (NAS100_USD, 2022-01-02 → 2026-04-19, 101,226 rows) using the same window as the other three instruments. OANDA TV-export panel `data/tv_exports/oanda/Striker_NAS100_v1_OANDA_NAS100USD_2026-05-08_74d8e.csv` landed (secondary feed; Pepperstone remains canonical). Striker DJ30 changelog gained a 2026-05-08 entry recording the split-out and the dd_protection C2 relock cross-ref (DJ30 bust attribution 40.9% → 44.4% under C2). Active cross-refs to the old NAS path repaired in `REPO_MAP.md`, `docs/briefs/striker_nas100_q_nas_1_results.md`, and `docs/briefs/striker_nas100_q_nas_3_mc_addition.md`. `tests/test_mc_anchors.py` + `tests/test_tv_export_loader.py` still pass (11/11).

- **Docs archive sweep — DJ30 verdict trail + Q-NAS-2 capture plan.** Joshua's call: "honor the convention but archive DJ30 findings". Existing convention (2026-05-07 sweep) keeps verdict trail in active `docs/methodology/findings/` even when underlying analysis code archives — this commit overrides that for the DJ30 cluster only, on the rationale that the underlying analysis code (Q-DJ30-1/2/3) archived in the same-day commit `a042b1c` and the verdict trail follows it. The 2026-05-02/03 closed cluster (USOIL, EURUSD, GBPUSD, USDCHF) stays active per convention. Moved 7 paths: 3 findings (`docs/methodology/findings/2026-05-06_dj30_{event_clustering,gap_magnitude,stop_cap}.md`), 2 gate audits (`docs/methodology/gate_audits/2026-05-06_q-dj30-{2,3}_*.md`), 1 brief dir (`docs/briefs/Q-DJ30-3/`), and `docs/striker_nas100/q_nas_2_capture_plan.md` (Q-NAS-2 closed today, no forward dependencies). All to mirrored paths under `archive/docs/`. `docs/striker_nas100/` directory removed (now empty). Cross-archive link repairs in 7 moved files: `../../../strategies/` → `../../../../strategies/` and similar `+1 ../` adjustments where paths cross the archive boundary; `../../../archive/analysis/` → `../../../analysis/` (the `archive/` prefix becomes redundant from inside `archive/docs/`); cross-stayed-active links like `../regime_robustness_gate.md` → `../../../docs/methodology/regime_robustness_gate.md`. Active-doc updates: REPO_MAP.md (manifest entries moved active → archive section), `docs/notion/repo_context.md` (tree + Q-DJ30-3 status note path).

- **Closed-Q archive sweep — Q-DJ30-1/2/3, dd_protection_trace.py, striker_nas100/ shell.** `analysis/Q-DJ30-1/`, `Q-DJ30-2/`, `Q-DJ30-3/` (all closed CLOSE/null or AMBIGUOUS/HOLD on 2026-05-06) moved to `archive/analysis/`. The 2-week cooldown set in `1e6687d` (§3 forbidden-moves discipline, target ~2026-05-20) was explicitly overridden by Joshua. `analysis/dd_protection_trace.py` moved with the same cohort — closed Q-DDP-1 era forensic tool, kept as reusable template per the ADR 2026-05-08-dd-trigger-c2-relock dissent record. `analysis/striker_nas100/` placeholder shell deleted (`__init__.py` only; q_nas_1 already at `archive/analysis/striker_nas100/`, q_nas_2 plan closed today). Path references swept in 9 active docs (REPO_MAP.md, docs/notion/repo_context.md, docs/methodology/1r_estimation.md, docs/methodology/findings/2026-05-06_dj30_{event_clustering,gap_magnitude,stop_cap}.md, docs/methodology/gate_audits/2026-05-06_q-dj30-{2,3}_*.md, docs/briefs/Q-DJ30-3/Q-DJ30-3.md). `analysis/` now contains only `oanda_stage1/` (test imports + Q-NAS-2 capture-plan reference) and `time_to_pass.py` (forward C2→C0 revert-trigger harness, quarterly cadence per ADR).

## 2026-05-07

- **Approach D archive consolidation.** Four per-domain `archive/` subtrees consolidated under a single top-level `archive/`: `analysis/archive/` → `archive/analysis/` (commit `821455b`, 39 files), `docs/methodology/archive/` → `archive/docs/methodology/archive/` (commit `a59985e`, 174 files; inner `analysis/archive/` and `analysis/msee/archive/` segments preserved as-is per the existing methodology-archive README), `strategies/striker/archive/` → `archive/strategies/striker/` (commit `068c295`, 1 file), `data/tv_exports/archive/` → `archive/data/tv_exports/` (commit `7cdd4d1`, 1 file). All four moves are pure renames (215 files, zero content change). Path references swept in 16 active docs (CLAUDE.md, observation_routing.md, rule_0.md, methodology/findings/*, data/reconciles/*, analysis/msee/watch_*) and 3 active code files (lib/mvd.py, scripts/msee_watchlist.py, analysis/Q-DJ30-1/build_calendar.py — all docstring/output references; no imports affected). ADR addendum-only modification on `docs/adr/2026-05-03-sentinel-gate-decision.md`. Historical CHANGELOG entries below describe state at time of writing — pre-2026-05-07 paths were the live paths at that point and are not retroactively rewritten. New `REPO_MAP.md` manifest at repo root classifies paths as `[A]ctive` / `[U]tility` / `[X]archived` / `[?]open-question`.

## 2026-05-05

- **Doc/code skew sweep across the 4-strategy lock surfaces.** Patched stale 3-strategy MC anchor numbers (`92.73 / 0.65 / 4.94`) and pre-Guardian-re-export 4-strategy numbers (`98.13 / 0.22 / 4.49`) to the canonical `97.88 / 0.22 / 4.55` across `dd_protection.py` provenance comments, `tests/test_mc_anchors.py` docstring, `analysis/striker_nas100/q_nas_1_pyramid_hypothesis.py`, `docs/briefs/striker_nas100_q_nas_1_results.md`, and the Striker v4.5 CHANGELOG entry. Forward-pointer notes added to Guardian / Aegis CHANGELOGs and `docs/methodology/1r_estimation.md`. Operational tooling scope (NAS100 deliberately out of `firm_rules`/`dd_protection`/`accounts`/`cli` while candidate-not-deployed) documented in `CLAUDE.md`. Tooltip / lineage cleanup in `strategies/striker/striker_nas100_v1.pine` (v4.4 → v4.5 references where DJ30 family is currently locked) and the `lib/mvd.py` docstring example. README and `portfolio_mc.py` module docstring corrected: invocation is `python portfolio_mc.py` (top-level module — no `prop_firm_pipeline` namespace per `pyproject.toml`); TV-export layout documented as `data/tv_exports/{pepperstone,oanda}/<full-name>.csv` (MVD identity gate enforces the seven-field shape). Aegis [Unreleased] BOJ April 28 watch and Guardian [Unreleased] v5.4 Pepperstone re-MC closed (former: window elapsed; latter: moot under the 4-strategy lock).

- **`scripts/verify_lock_anchors.py` retired.** Tool intended to back Rule 6 (doc/code skew on lock) was silently broken in three ways: regex on `firm_rules.py` no longer matched after the 2026-04-26 unified-allocation refactor (`"challenge": {…}` literal block became a dict-comprehension); version extraction read `OANDA_PANELS` though Pepperstone is the lock-anchor source since 2026-04-26; hardcoded `("guardian","striker","aegis")` had no NAS100 awareness. Output (`code=None` for every risk) provided false reassurance, which is anti-Rule-6. Removed; Rule 6's process intent is preserved manually. Untracked `analysis/skew_audit/2026-05-05.md` orphan and the now-empty `analysis/skew_audit/` directory removed.

- **Closed-investigation archive sweep (re-establishes the 2026-04-29 `analysis/archive/` convention).** Four post-2026-04-30 investigation tracks closed without action and moved to `analysis/archive/`: `usoil/` (Q-USOIL-3 closed 2026-05-02 — no directional edge in OANDA M15 conditional on the vol-gate), `eurusd_lnyo/` (H-PDSB killed at G2 + H-NYFBO kill, 2026-05-02), `gbpusd_lon/` (H-LORB Phase A aborted at G1 — random-walk noise / strict gate, 2026-05-03), `usdchf_sentinel/` (Sentinel gate rejected as a fourth strategy — PF 1.026, perm-p 0.944, see `docs/adr/2026-05-03-sentinel-gate-decision.md`). Orphan data `data/tv_exports/pepperstone/USOIL_pepperstone_m15_2025-06-02_to_2026-05-01.csv` moved to `data/tv_exports/archive/`. Path references updated in 11 live docs (findings + reconcile + archive gate audits) and in the moved scripts' own docstrings/comments for self-consistency. Verdicts and decision docs remain at their `docs/methodology/findings/` and `docs/adr/` locations as canonical references.

## 2026-04-30

- **Strategy-research-phase methodology archived; Rule 0 promoted standalone.** The methodology layer (INQHIORI ⊕ The Algorithm framework, Pre-Q gates, Case B audits, backfill discipline, mandatory header ordering, MVD framing) retired with the strategy-research phase (Feb–Apr 2026 — locks on G v5.5 / S v4.4 / A v4.3 delivered, FXIFY $200K live). Corpus moved to `docs/methodology/archive/` with manifest README, resurrection rule, and 2026-07-29 review gate. Rule 0 (audit-first; cross-phase track record 2026-04-17 / 2026-04-27) promoted to standalone at `docs/rule_0.md`. MVD principle folds in as Rule 0 corollary #4. `lib/mvd.py` assertion library stays live as execution infrastructure (multi-firm expansion makes it more load-bearing, not less). `docs/methodology/observation_routing.md` and `1r_estimation.md` remain active outside the archive. New rules require observed execution-phase failure to earn existence; pre-emptive scaffolding rejected as methodology-rebound trap.
- **Archive scope.** Methodology dirs: `docs/methodology/{mvd.md,data_provenance/,findings/,gate_audits/,identify_corpus/,msee/}`. Analysis dirs: `analysis/{notice_phase/,inquire_phase/,archive/,msee/archive/,skew_audit/2026-04-27.md}`. Research-phase scripts: `scripts/{var_alloc_inquire/,audnzd_phase1_*.py,audnzd_phase2_structural.py,audnzd_phase3_*.py,identify/}`. Live retained: `analysis/{dd_protection_trace.py,time_to_pass.py,msee/daily_strategy_returns.*,msee/watch_2026-04-27.*}`, `scripts/{dryrun_aegis_v4_3.py,fetch_oanda_bars.py,lock_event_hook.py,msee_watchlist.py,run_v55_validation.py,verify_lock_anchors.py,build_us_releases.py}`, `lib/mvd.py`.
- **Rewires.** `lib/mvd.py` docstring → points at `docs/rule_0.md`; templates `docs/templates/{bust_analysis,calibration_brief,lock_decision}.md` Methodology field → `docs/rule_0.md` (corollary #4); `scripts/msee_watchlist.py` and `analysis/msee/watch_2026-04-27.md` watch_list.md path → archive path; `docs/methodology/observation_routing.md` two analysis/notice_phase refs → archive paths; `CLAUDE.md` Methodology references section gets Rule 0 (top) and archive README (bottom). Historical CHANGELOG entries and ADRs left as frozen record.

## 2026-04-29

- **Archive sweep — closed-no-action analysis moved out of the active tree.** `analysis/archive/` now holds the closed Q-series (Q3 / Q5 / Q11 / Q12 / Q14 / Q15) plus pre-locked bar-analysis (`bar_analysis.py`, `bar_data_high_leverage.md`, four PNGs, `regime_by_year.csv`), `1r_diagnosis.py` (codified into `docs/methodology/1r_estimation.md`), `striker_pyramid_decomposition.py`, and `correlated_day_check.py` + `worst_joint_days.csv`. `analysis/msee/archive/` holds the H1–H10 individual hypothesis runs (concluded — no Action). Live MSEE substrate retained at `analysis/msee/`: `daily_strategy_returns.*` (watchlist substrate) and `watch_*.{md,json}` (dated digests). README at each archive dir explains provenance.
- **Strategy file extension `.txt` → `.pine`.** `strategies/{aegis,guardian,striker}/*.txt` renamed to `.pine` (canonical Pine Script extension). Updated 11 reference sites: 3 strategy CHANGELOGs, 3 ADRs (`2026-03-01-aegis-session-selection`, `2026-04-17-guardian-v5.1-architecture`, `2026-04-17-striker-v4.3-pyramid`), `docs/methodology/mvd.md` (worked example + audit-table row 7), `docs/methodology/1r_estimation.md`, `docs/overlays/guardian_conflict_risk.md`, `scripts/dryrun_aegis_v4_3.py`, this CHANGELOG, archived `analysis/archive/1r_diagnosis.py`, and `analysis/notice_phase/findings_2026-04-26.md`. `lib/mvd.py` docstring already used `.pine`. Grep confirms zero `.txt` strategy refs remain.
- **`Market Ecology, the Storage Effect, and Evolutionary Dynamics.md` removed from repo.** Source paper for MSEE was 41KB at the repo root, producing zero operational decisions. Removed; MSEE `framework.md` and `open_questions.md` updated to mark the source as external (Joshua retains a local copy). MSEE research track itself stays — it's the framework around the watchlist.
- **Pepperstone-canonical Action-routing bottleneck lifted (executive decision, Joshua).** Prior rule "no Action routing on OANDA-only findings" — encoded in `docs/methodology/identify_corpus/2026-04-26/AMENDMENT_oanda_rescope.md` and propagated through MSEE, Notice, and Inquire findings — superseded. New policy: OANDA findings can route to Action proposals; Joshua validates in TradingView against Pepperstone bars before any code/lock change. CLAUDE.md headline MC numbers (92.73% / 0.65% / 4.94% p99 DD) remain Pepperstone-anchored as lock-decision artifacts. Source-feed tagging (`canonical_status`) discipline retained — silent substitution still forbidden. Supersession note added to AMENDMENT (canonical source); live methodology docs updated (`identify_corpus/README.md`, MSEE `framework.md` / `open_questions.md` / `watch_list.md`, Notice `findings_2026-04-27.md`, Inquire `findings_q_t_2026-04-27.md`). Historical ADRs and closed REJECTED briefs preserved as frozen records.
- **Q-T (Tuesday-cohort bar-level concurrent-loss) — closed with watchlist tripwire.** OANDA-proxy finding logged: Tuesday-K=3 windows (G ∩ S ∩ A simultaneously session-active) show 8.64% bar-level concurrent-loss vs Mon-K=2's 0.74% headline; K-matched residual +2.73pp, p ≪ 0.05. An Action proposal (uniform-s sizing test on Tue-K=3 windows with Pareto adoption gates) was drafted under the lifted Pepperstone-canonical gate, then **closed without execution** — Joshua's call: an OANDA-proxy bar-stat without a live-PnL signal is not enough to justify the lock cycle, even with a P&L-test gate built into the proposal. The "leading indicator with P&L gate" framing is the rationalizing form of the Iran/Hormuz overlay-discipline trap. Action proposal preserved verbatim in `analysis/inquire_phase/findings_q_t_2026-04-27.md` for trail; new Disposition section ahead of it. Watchlist tripwire added to `docs/methodology/msee/watch_list.md`: **live FXIFY** Tuesday daily-portfolio-R std > 1.5 × max(Mon std, Thu std), n ≥ 30 trade-days each cohort, rolling 6-month — re-opens the proposal if tripped. Variance-not-mean because tail-concentration appears as second-moment signal first. Reports `insufficient_data` until live data accumulates.
- **Memory observations saved.** Three durable methodology lessons added: (1) anchor sizing-rule effect on K-matched residual not headline ratio (combinatorial-artifact discipline); (2) soften Pareto adoption criteria against MC sampling noise (one-soft / one-strict / two-floor table shape; strict-inequality concentrated on highest signal-to-noise metric, typically p99 DD); (3) "leading indicator with P&L gate" is the rationalizing form of Iran/Hormuz — a backtest gate validates backtest translation, not live-PnL pay; correct response is watchlist + forward live-PnL tripwire. Two-tier canonical memory updated to reflect the lifted Pepperstone-canonical gate.

## 2026-04-28

- Stale-artifact sweep across the main checkout: Guardian Pine header risk-comment refreshed `0.30%` → `0.34%` (`strategies/guardian/guardian_gold_v5.5.pine`); Striker `stopAtr` tooltip refreshed `LOCKED: 1.35` → `LOCKED: 1.25` with v4.3→v4.4 derivation note (`strategies/striker/striker_dj30_v4.4.pine`); 2026-04-17 portfolio-allocations ADR's funded-ramp risk bullet marked superseded by the same-day unified-allocation decision; Guardian conflict overlay's "0.55% baseline" annotated as pre-v5.1-era to resolve the timing mismatch with v5.1's 0.30% cold-start; Aegis CHANGELOG's "post-v4.3 portfolio MC re-run" Unreleased item moved into the v4.3 entry as completed (Aegis bust attribution 47% → 27.6% at canonical 92.73% pass / 0.65% bust config).
- Notice-phase compression cleanup: deleted closed-bucket standing JSON outputs per `docs/methodology/observation_routing.md` rule (`o1`/`o3`/`o4`/`o5`/`o6`/`phase0_2026_04_27.json`, `rule0_sanity.json`, `B3_intraday_vs_daily.json`). Retained B1/B2 (Forward) plus `figures/B1_rolling_corr.png`. All deleted outputs are regenerable from the sibling `.py` scripts.
- v5.5 validation halt (`docs/historical/v5_5_validation_halt_2026-04-21.md`) annotated with resolution pointer to the 2026-04-23 lock; `run_v55_validation.py` moved out of `docs/historical/` into `scripts/`. Repo-root markdown stray and `data/bar_data/` corpus tracked.

## 2026-04-27

- `notice: 2026-04-27 OANDA-proxy bar-corpus scan` — second Notice run on the 2026-04-26 OANDA-proxy Identify corpus. Phase 0 PASS, five extractions (O1–O6) with `canonical_status = "PROXY"` end-to-end (Action routing forbidden on this corpus per the OANDA rescope amendment). Findings doc: `analysis/notice_phase/findings_2026-04-27.md`. Closed-bucket standing outputs from this run were removed in the 2026-04-28 sweep.

## 2026-04-26

- **Identify-corpus 2026-04-26 (OANDA-proxy)** — full INQHIORI loop iteration on the OANDA-proxy bar corpus (XAUUSD/US30/USDJPY 15m, 2022→2026). Materials under `docs/methodology/identify_corpus/2026-04-26/` including `README.md`, `phase0_log.json`, and `AMENDMENT_oanda_rescope.md` (formalizes the two-tier canonical: Pepperstone authoritative, OANDA proxy; D-test forbidden on proxy alone).
- **Inquire-phase findings (var-alloc, AUDNZD)** — `docs/methodology/findings/2026-04-26_var_alloc_observables_stage0.md`, `2026-04-26_var_alloc_dd_state_REJECTED.md`, `2026-04-26_audnzd_REJECTED.md`. Both verdicts 4A REJECTED.

## 2026-04-25 (methodology compression)

- Added `docs/methodology/observation_routing.md` — three-bucket gate (Closed/Action/Forward) replacing the prior Notice/Inquire two-phase framework. Algorithm-driven simplification (Question / Delete / Simplify) — Notice's protective benefit was redundant once Rule 0, Iran/Hormuz overlay-trigger discipline, and documented re-MC triggers were in place. Standing closed-bucket JSON/figure/CSV outputs deleted in the same compression; B-series (Forward) retained.
- Updated `docs/methodology/1r_estimation.md` — five follow-ups in the same day: equity-compounding clarification on Guardian v5.5 1R (median = 0.3405%, hits designed 0.34% to 4dp); live-sizing Rule 0 cross-check via `accounts.calc_multiplier`; Striker pyramid decomposition (corrects the 17%-inflation framing — initial entries +2.9%, pyramid layers +76.6%); pyramid 3.50× multiplier verified directly from layer-1/layer-2 qty pairs; pre-staged Forward question on within-day per-trade hard-cap vs leave-and-monitor-live for the 2025-02-07 fresh-peak single-trade outlier.
- Q5 break-window P&L analysis: ESCALATE Q3 (positive-tail co-movement, dd_protection rule persists at canonical config).

## 2026-04-25 (retrofit)

- Added `docs/adr/2026-04-25-mvd-retrofit.md` — ADR scoping the production-code retrofit: `portfolio_mc.py`, `dd_protection.py`, audit row #10.
- Updated `docs/methodology/mvd.md` — added audit row #10 (DD label ambiguity, Identity-class), refreshed summary stats `9 → 10 instances`, `67% → 70% identity`, `6/9 → 7/10 single-line`. Top-of-section "67%" reference at line 42 also updated. Bottom MVD-attest refreshed.
- `portfolio_mc.py` retrofit (separate commit): `implied_1r` returns `(r1, fell_back)`; canonical-config silent-fallback path now raises via `assert_no_fallback`. `load_trades` validates input panels via `assert_min_rows` + `assert_window`. Maps to methodology family **Contract** (audit instance #1) and **Cardinality** (audit instance #2).
- `dd_protection.py` retrofit (separate commit): module-load `_validate_protection_rule()` runs `assert_guard_fired` (just-above trigger) + `assert_no_fallback` (just-below trigger). Catches future drift in `DD_TRIGGER` / `DD_SCALE`. Maps to methodology family **Contract**.
- **MVD-attest:** numbers cited in the retrofit ADR (`~2 hrs`, `9→10`, `67%→70%`) trace to: time tracked on the retrofit conversation 2026-04-25; audit table at `docs/methodology/mvd.md` updated in this PR.

## 2026-04-25

- Added `scripts/dryrun_aegis_v4_3.py` — MVD helper sanity gate. Exercises all 9 helpers in `lib/mvd.py` against the canonical Pepperstone 52mo Aegis v4.3 CSV (123 trades, Jan 2022 → Apr 2026). All 5 canonical numbers reproduce: net $178,208.42, PF 4.186, WR 60.16%, intra-trade DD 5.01%, trade-close DD 3.82%.
- **Finding (Identity-class candidate, audit instance #11):** "DD" without a labeling qualifier is ambiguous. The MVD page cited 5.01% (intra-trade); `aegis_CHANGELOG.md` cited 3.76% (trade-close). Both are correct, just different metrics. Harness asserts each explicitly with the qualifier in the label string.
- **MVD-attest:** the harness output is the producing source for the canonical-reproduction claim; identity assertions are within the first 5 lines of `main()` (filename-parsed symbol/broker/version verified before any metric is computed).

## 2026-04-24

- Added `docs/adr/2026-04-24-mvd-discipline.md` — ADR formalizing the Minimum Viable Defense discipline for load-bearing artifacts crossing the live capital boundary.
- Added `docs/methodology/mvd.md` — methodology reference: 5 families, worked examples, 9-instance audit table, producer/consumer rules.
- Added `lib/mvd.py` — assertion library (9 helpers across 5 families: cardinality, identity, contract, cross-source, code-vs-doc).
- Added `docs/templates/lock_decision.md` — lock brief template with verification preamble and MVD-attest section.
- Added `docs/templates/calibration_brief.md` — calibration brief template with identity-assertion preamble.
- Added `docs/templates/bust_analysis.md` — bust analysis template requiring script-generated event-count + $ attribution.
- **MVD-attest:** the audit table in `docs/methodology/mvd.md` is the source for all numbers cited in the ADR (`67%`, `6/9`, `9 instances`, etc.); numbers traced on first read.
